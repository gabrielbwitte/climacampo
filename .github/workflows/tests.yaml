name: Test

on: [push]

jobs:
    test_build:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4
        - name: build
          run: cargo build --release --verbose

        
